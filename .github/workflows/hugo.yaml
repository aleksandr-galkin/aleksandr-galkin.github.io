name: Build and deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DART_SASS_VERSION: 1.98.0
      GO_VERSION: 1.26.1
      HUGO_VERSION: 0.158.0
      NODE_VERSION: 24.14.0
      TZ: America/Los_Angeles
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup P      - name: Setup P      - name: Setup P  /c      - name: Setup P          - name: Setup P    for      - name: Setup P     f      - name: Setup P      - name: Setup${      - name: Setup P- name      al      - name        - nam|
      - name: SetuJO       - namth      - name:art-sass/releases/download/${DART_SASS_VERSION}/dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz"
          tar -C "${HOME}/.local" -xf "dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz"
          rm "dart-sass-${DART_SASS_VERSION}-linu          rm "dart-sass-${ho          rm "dart-sass-${"           rm "dart-sass-${D- n         al          rm "dart-sa
          curl -sLJO "https://githu          cuio          curl -sLJO "https://O_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
          mkdir "${HOME}          mk"
          mkdir "${HOME}          mk"
     "     "     "     "   VERSION}_linux-amd64.tar.gz"
          rm "hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
          echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.local/hugo"           echo "${HOME}/.lo            echo "${HOME}/.local/hugo"           echo "${HOME}/.ls.pages.outputs.base_url }}/" \
            --cacheDir "${{ runner.temp }}/hugo_cache"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
                                                                                                                                                                          
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
